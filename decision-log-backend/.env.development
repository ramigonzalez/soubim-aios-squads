# Development Environment Configuration
# This file is used when running the backend in development mode
# It uses SQLite for quick local development without needing PostgreSQL

# Database - Using SQLite for development (no PostgreSQL required)
# For production, override with PostgreSQL: postgresql://user:pass@localhost:5432/decisionlog
DATABASE_URL=sqlite:///./decisionlog_dev.db

# JWT Configuration
JWT_SECRET_KEY=dev-secret-key-at-least-32-characters-long-for-jwt-dev
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=10080

# API Keys (use test values for development)
ANTHROPIC_API_KEY=sk-ant-test-key-development
TACTIQ_WEBHOOK_SECRET=whsec_test_secret_dev

# Environment
ENVIRONMENT=development
DEBUG=true
DEMO_MODE=true

# CORS - Allow localhost on multiple ports
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:3000"]

# Optional: Sentry DSN (leave empty to disable in development)
SENTRY_DSN=
