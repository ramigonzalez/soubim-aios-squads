# Story 6.4 — Project CRUD Completion

**Epic:** 6 (Project Management)
**Priority:** High | **Effort:** ~2 days
**Status:** Done

## Description
Frontend pages (ProjectCreate, ProjectEdit) and mutation hooks exist but are dead code — backend endpoints missing, routes not registered. Users cannot create, edit, or delete projects.

## Acceptance Criteria
- [x] POST /api/projects creates a project (director-only)
- [x] DELETE /api/projects/{id} soft-deletes via archived_at (director-only)
- [x] /projects/new route renders ProjectCreate page
- [x] /projects/:id/edit route renders ProjectEdit page
- [x] ProjectDetail shows Edit/Archive buttons for directors
- [x] Archive has confirmation dialog

## Tasks

### Backend
- [x] POST /api/projects — Create project with name, description, project_type
- [x] DELETE /api/projects/{id} — Soft-delete via `archived_at = now()`

### Frontend
- [x] Register routes in App.tsx (`/projects/new`, `/projects/:id/edit`)
- [x] Fix ProjectCreate.tsx — wire up useCreateProject with onSubmit/onCancel
- [x] Fix ProjectEdit.tsx — wire up useUpdateProject with existing project data
- [x] Add Edit/Archive buttons to ProjectDetail header (admin-only)
- [x] Archive confirmation dialog

## File List
- `decision-log-backend/app/api/routes/projects.py` — POST + DELETE endpoints
- `decision-log-frontend/src/App.tsx` — route registrations
- `decision-log-frontend/src/pages/ProjectCreate.tsx` — rewired with hooks
- `decision-log-frontend/src/pages/ProjectEdit.tsx` — rewired with hooks
- `decision-log-frontend/src/pages/ProjectDetail.tsx` — Edit/Archive buttons + dialog

## Verification
Navigate to `/projects/new`, create project, edit it, archive it.
